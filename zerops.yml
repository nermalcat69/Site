zerops:
  - setup: sitedev
    build:
      base: nodejs@20
      buildCommands:
        - yarn
        - yarn build
      deploy:
        - .nuxt
        - .output
      cache:
        - node_modules
        - yarn.lock
    run:
      start: yarn start